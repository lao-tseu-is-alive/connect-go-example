package main

import (
	"context"
	"fmt"
	"log"
	"net/http"
	"os"

	"connectrpc.com/connect"
	greetv1 "github.com/lao-tseu-is-alive/connect-go-example/gen/greet/v1"        // generated by protoc-gen-go
	"github.com/lao-tseu-is-alive/connect-go-example/gen/greet/v1/greetv1connect" // generated by protoc-gen-connect-go
	"github.com/lao-tseu-is-alive/go-cloud-k8s-common-libs/pkg/config"
	"github.com/lao-tseu-is-alive/go-cloud-k8s-common-libs/pkg/golog"
)

const (
	APP            = "greetClient"
	VERSION        = "0.0.2"
	defaultName    = "connectRPC"
	defaultIp      = "127.0.0.1"
	defaultPort    = 8080
	defaultLogName = "stderr"
)

func main() {
	logWriter, err := config.GetLogWriter(defaultLogName)
	if err != nil {
		log.Fatalf("💥💥 error getting log writer: %v'\n", err)
	}
	logLevel, err := config.GetLogLevel(golog.InfoLevel)
	if err != nil {
		log.Fatalf("💥💥 error getting log level: %v'\n", err)
	}
	l := golog.NewLogger("simple", logWriter, logLevel, APP)
	l.Info("🚀 Starting", "app", APP, "version", VERSION)
	nameToGreet := defaultName
	if len(os.Args) > 1 {
		nameToGreet = os.Args[1]
	}
	l.Info("will say hello to ", "name", nameToGreet)
	serverAddress := fmt.Sprintf("%s:%d", defaultIp, defaultPort)
	l.Info("Will call Greet to server :", "listenAddress", serverAddress)

	client := greetv1connect.NewGreetServiceClient(
		http.DefaultClient,
		"http://localhost:8080",
		connect.WithGRPC(), // a single line allow using the gRPC protocol instead of the Connect protocol (default)
	)
	res, err := client.Greet(
		context.Background(),
		&greetv1.GreetRequest{Name: nameToGreet},
	)
	if err != nil {
		log.Println(err)
		return
	}
	log.Println(res.Greeting)
}
